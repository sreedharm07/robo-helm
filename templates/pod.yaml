apiVersion: v1
kind: Pod
metadata:
  name: {{ .Values.component }}
  labels:
    app.kubernetes.io/name: {{ .Values.component }}
spec:
  containers:
    - name: {{ .Values.component }}
      image: 471200138067.dkr.ecr.us-east-1.amazonaws.com/{{ .Values.component }}